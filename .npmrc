# React Native's Metro bundler cannot reliably traverse pnpm's symlinked
# store: it resolves a package to two different copies (React loaded twice) or
# fails outright on transitive Expo modules. A hoisted node_modules is the
# supported layout for RN, and is what the other apps in this account use.
node-linker=hoisted
