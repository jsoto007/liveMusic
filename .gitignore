# Secrets — never commit. .env.example carries key names only.
.env
.env.*
!.env.example
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
instance/
*.sqlite3

# Node
node_modules/
dist/
.turbo/
*.tsbuildinfo

# Expo / native — this is a managed-workflow project; `ios/`/`android/` are
# `expo prebuild` output, not source, and reappear (Pods and all) the moment
# someone runs the app natively.
.expo/
apps/mobile/ios/
apps/mobile/android/

# Editors / OS
.DS_Store
.idea/
.vscode/
